#Behind scenes of set and dict
#hashing elemnts stored in table usin (key%m) index
#if collision happens we can solve it usng channing
#channing consists of linear probing(checks next index and stores if free)
#                     qudaratic probing stores in i*i index
#                     double hashing stores element is next hash index


########In default dict we can store default values as lambda:0
